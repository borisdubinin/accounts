server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://postgres_accounts:5432/accountsdb
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: validate

logging:
  level:
    org.example: DEBUG