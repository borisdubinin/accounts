spring:
  datasource:
    url: jdbc:postgresql://postgres_accounts:5432/accountsdb
    username: postgres
    password: postgres

logging:
  level:
    org.example: INFO

springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false